\section{Anharmonic transport in silicon nanowires}
\label{sec:res_nw}
% ======================================================================

We study two hydrogen-passivated [100] silicon nanowires: \texttt{d5a}
($\mathrm{Si}_9\mathrm{H}_{12}$ per cell, $63$ DOF) and the wider
\texttt{d11a} ($135$ DOF). Their harmonic transport is physical
(\cref{fig:res_ballistic_curves})\todo[inline]{Remove from the plot the cnt or fold the cnt}: the ballistic conductance rises with
temperature, and \texttt{d11a} carries
$\sim1.5\times$ the conductance of \texttt{d5a}.

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.48\linewidth]{transport_sweeps/ballistic_vs_T_d5_d11.pdf}\hfill
  \includegraphics[width=0.48\linewidth]{transport_sweeps/ballistic_vs_length_d5_d11.pdf}
  \caption{Ballistic conductance of the \texttt{d5a} and \texttt{d11a}
  silicon nanowires. Left: $G(T)$; right: $G$ versus device length.}
  \label{fig:res_ballistic_curves}
\end{figure}

\paragraph{Mode-resolved anharmonic physics.} A phono3py analysis of the
bulk-silicon FC3 (\cref{fig:res_modephysics}) is performed: phonon lifetimes
have a median of $4.2$~ps with a $\tau\!\propto\!\omega^{-2}$ envelope.
$50\%$ of $\kappa$ is carried by phonons with mean free path $>134$~nm
and $93\%$ by acoustic modes. Umklapp accounts for $0.44$ of the scattering at $300$~K,
and the mode Gr\"uneisen parameters have mean $|\gamma|=1.04$, matching the
experimental Si value and validating the FC3 anharmonicity strength.
Only $1.3\%$ of bulk $\kappa$ comes from phonons with mean free path below $10$~nm. The
$\sim1$~nm nanowire cores therefore sit far below the bulk
phonon-MFP spectrum: they are mostly performing boundary scattering and
retain almost none of the bulk anharmonic-limited $\kappa$.

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.48\linewidth]{transport_sweeps/phph_physics_si.pdf}\hfill
  \includegraphics[width=0.48\linewidth]{transport_sweeps/phph_NU_gruneisen_si.pdf}
  \caption{Bulk-silicon mode-resolved anharmonic physics (phono3py,
  FD FC3). Left: lifetimes, $\kappa$-accumulation versus mean free path,
  and the acoustic-mode $\kappa$ fraction. Right: Normal/Umklapp split
  and the mode Gr\"uneisen distribution (mean $|\gamma|=1.04$).}
  \label{fig:res_modephysics}
\end{figure}

At physical broadening and moderate vertex strength the SCBA converges
cleanly and gives the physical $G_\mathrm{anh}<G_\mathrm{ball}$.

\paragraph{What the open-wire SCBA requires.} Two conditions are necessary for the
nanowire self-consistency to reach this physical value. First, the three-phonon
vertex must be built from an \emph{acoustic-sum-rule-projected} FC3: the raw
mass-weighted FC3 violates translational invariance on the open device and couples
the scattering vertex into the soft acoustic/twist subspace, so the unprojected
$\lambda{=}1$ SCBA diverges where the projected one converges (the projection drops
$\sim25\%$ of $\|\Phi^{(3)}\|$ and leaves the ballistic baseline unchanged). Second,
the broadening must be physical: an over-large diagnostic
$\eta_w\sim0.7$--$2$\,THz crushes the low-frequency acoustic channels that carry the
wire's heat, collapsing $G_\mathrm{ball}$ by up to two orders of magnitude and
producing the spurious $G_\mathrm{anh}>G_\mathrm{ball}$ of early sweeps; at physical
$\eta_w$ the resistive ordering is restored.

\Cref{tab:res_asr_compare} makes the projection effect quantitative on a single
representative \texttt{d5a} point (one cell, $\eta_w\approx0.11$~THz, full coupling
$\lambda=1$): the raw vertex diverges --- residual past $10^2$, $620$ causality-%
violating frequency points, the guard aborting after ten iterations --- while the
projected vertex converges monotonically in thirty iterations to residual
$8.8\times10^{-4}$ with a bounded $\max|\Sigma^R|\approx136$~THz$^2$ and no causality
violations; the projection lowers the stacked-FC3 Frobenius norm by $25\%$ and leaves
the ballistic conductance identical to seven figures.

\begin{table}[htb]
  \centering
  \caption{Projected vs.\ raw FC3 at full coupling ($\lambda=1$) on \texttt{d5a}
  (one transport cell, $\eta_w\approx0.11$~THz). The raw vertex diverges; the
  ASR-projected vertex converges. Conductances in $\mathrm{W\,m^{-2}\,K^{-1}}$.}
  \label{tab:res_asr_compare}
  \begin{tabular}{lcccccc}
    \toprule
    FC3 vertex & converged & iters & residual & causality & $G_\text{ball}$ & $G_\text{anh}$ \\
    \midrule
    raw (no ASR)   & no  & $10$ (abort) & $1.2$              & $620$ viol.\ pts & $2.15\times10^{7}$ & (diverged) \\
    ASR-projected  & yes & $30$         & $8.8\times10^{-4}$ & none             & $2.15\times10^{7}$ & $2.20\times10^{7}$ \\
    \bottomrule
  \end{tabular}
\end{table}

At this single-cell, full-coupling point the converged anharmonic conductance sits
marginally \emph{above} the ballistic value ($G_\mathrm{anh}/G_\mathrm{ball}=1.02$,
with $\sim1.9\%$ heat-flow non-conservation): the finite-$\eta$ single-cell artefact
of \cref{sec:res_approx}, not a physical anti-resistance. Across temperature the band
widens mildly with $T$ as the Bose factors grow ($0.98/1.02/1.09$ at $200/300/400$~K,
\cref{tab:res_anh_T}); the strict, current-conserving $G_\mathrm{anh}<G_\mathrm{ball}$
is recovered at moderate coupling ($\lambda=0.3$ gives $0.94$), in a multi-cell device,
or in the $\eta\to0$ limit of \cref{sec:res_eta0}.

\begin{table}[htb]
  \centering
  \caption{Projected-FC3 SCBA on \texttt{d5a}, one transport cell, $\lambda=1$,
  $\eta_w\approx0.22$~THz (at the in-run broadening). Conductances in
  $\mathrm{W\,m^{-2}\,K^{-1}}$.}
  \label{tab:res_anh_T}
  \begin{tabular}{cccc}
    \toprule
    $T$ (K) & $G_\mathrm{ball}$ & $G_\mathrm{anh}$ & $G_\mathrm{anh}/G_\mathrm{ball}$ \\
    \midrule
    $200$ & $1.97\times10^{7}$ & $1.92\times10^{7}$ & $0.98$ \\
    $300$ & $2.15\times10^{7}$ & $2.19\times10^{7}$ & $1.02$ \\
    $400$ & $2.24\times10^{7}$ & $2.45\times10^{7}$ & $1.09$ \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Width dependence of the soft mode.} The near-zero twist is specific to the
narrow \texttt{d5a}. The wider \texttt{d11a} has no imaginary modes and its lowest
non-translational mode sits at $1.07$~THz, two orders of magnitude above
\texttt{d5a}'s $0.027$~THz twist, so the Bose-enhanced infrared stiffness is absent and
its SCBA is well conditioned: the finite-$\eta$ vertex-continuation converges in
$37$ iterations at $\lambda=0.3$ and stays stable to $\lambda=0.6$ (these are
low-coupling, finite-$\eta$ convergence points, not the physical conductance, which
is the $\eta\to0$ result of \cref{sec:res_eta0}). \texttt{d11a} is therefore the
better-conditioned structure for production nanowire transport. A third wire,
\texttt{d17a}, was
discarded: its force constants as fit carry $86$ imaginary modes at $\Gamma$ down to
$-34$~THz --- a fit-quality artefact of training on too few rattled supercells,
resolved by refitting rather than by the solver, and a reminder that the harmonic
input must be vetted before any anharmonic transport run.

A self-energy-level cutoff sweep on the \texttt{d5a} and \texttt{d11a}
nanowires (\cref{fig:res_cutoffs}) quantifies the truncations: the FC3
magnitude threshold converges quickly for \texttt{d5a}
($10^{-3}\to0.7\%$ relative change in $\Sigma^<$) but the wider
\texttt{d11a} needs a tighter threshold. A diagonal-$G$ approximation inside the
bubble is catastrophic ($5\times$ for \texttt{d5a}, $30\times$ for
\texttt{d11a}). The dominant cutoff is the Green's-function range, not
the vertex magnitude.

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.6\linewidth]{transport_sweeps/cutoff_sse_d5_d11.pdf}
  \caption{Self-energy sensitivity to the FC3 magnitude cutoff and to a
  diagonal-$G$ approximation for the \texttt{d5a} and \texttt{d11a}
  nanowires. The off-diagonal Green's-function content dominates.}
  \label{fig:res_cutoffs}
\end{figure}

Perofrming a rattle-amplitude sweep for the
\texttt{d5a} nanowire, the twist frequency stays real and stable
($0.5$--$1.0$~THz at $q_z=0.02$) at every amplitude ($0.015$--$0.08$~\AA),
with only fit-noise scatter --- never imaginary. The \texttt{d5a} twist is thus
robust.

% ======================================================================
