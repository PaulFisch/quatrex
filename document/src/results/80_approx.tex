% ======================================================================
\section{Cost of the numerical approximations}
\label{sec:res_approx}
% ======================================================================

The transport numbers above are computed under a hierarchy of controlled
approximations -- a finite numerical broadening, a truncated frequency grid,
real-space cutoffs on the FC3 vertex and the Green's function, and, optionally, a
low-rank compression of the vertex. This section quantifies how each affects the
converged conductance, so that every reported number carries a known systematic.

\paragraph{Finite broadening: why it is not the physical answer.}
The broadening $\eta$ of \cref{eq:system} is a numerical regulator, not physics, and
the conserving conductance is defined only as $\eta\to0$ (\cref{sec:res_eta0}).
Finite-$\eta$ transport numbers are reported nowhere in this chapter as a result;
they are collected here only to show that they are biased and, crucially, that the
bias cannot be removed by extrapolation. Its systematic is much larger than the
lead-balance residual suggests: on the \texttt{cnt33} tube at matched $\eta$ the
lead balance stays $\sim10^{-5}$ throughout, yet the anharmonic ratio
% REVIEW(kept from remote): eta enters as i*eta added to omega^2, so its unit is THz^2 -- applied here
$G_{\rm anh}/G_{\rm ball}$ swings $0.79\to0.92$ across $\eta=0.30$--$0.90$\,THz$^2$
(\cref{fig:res_cnt_T}), so a single broadening over-broadens the spectral function
and \emph{under}-estimates the scattering (a single point at $\eta=0.45$\,THz$^2$
reads only a $\sim16\%$ reduction). Worse, the extrapolation overshoots: the
$\eta\ge0.30$ window extrapolates to $G_{\rm anh}/G_{\rm ball}\approx0.70$, whereas
the directly converged $\eta\to0$ fixed point gives $0.574$ (\cref{sec:res_eta0}) ---
the finite-$\eta$ family does not connect smoothly to the $\eta=0$ limit, so even an
$\eta\to0$ fit of finite-$\eta$ data is wrong by $\sim20\%$. The same lesson is
sharper on the soft \texttt{d5a} wire, where a single cell at finite $\eta$ returns
the unphysical $G_{\rm anh}/G_{\rm ball}>1$ (\cref{tab:res_anh_T}): finite broadening
can violate the resistive bound outright. The only defensible transport number is the
% REVIEW(merge): remote text claimed the window extrapolates to 0.57; our sweep gives ~0.70 vs direct 0.574 -- verify the extrapolation number from the eta-sweep npz in the claims pass, and align sec:cons_ratio in app:conservation to the same story
$\eta=0$ fixed point; the mechanism and step-by-step verification are in
\cref{app:conservation}.

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.48\linewidth]{transport_sweeps/cnt33_temperature_g.pdf}\hfill
  \includegraphics[width=0.48\linewidth]{transport_sweeps/cnt33_temperature_ratio.pdf}
  \caption{Finite-$\eta$ (\emph{not} physical) anharmonic transport of the $(3,3)$
  carbon nanotube versus temperature at a single transport cell, shown to quantify
  the broadening bias. \emph{Left}: ballistic and anharmonic per-area conductance.
  \emph{Right}: the finite-$\eta$ ratio $G_\mathrm{anh}/G_\mathrm{ball}$ (falling
  % REVIEW(merge): 0.810 per the remote 0.71->0.810 reconciliation at 1 cell/300 K; confirm against the regenerated figure data
  $0.92\to0.810$ over $30$--$300$~K) sits well above the conserving $\eta\to0$ result
  of \cref{sec:res_eta0} ($0.75\to0.57$); the gap is the over-broadening systematic.}
  \label{fig:res_cnt_T}
\end{figure}

\paragraph{Frequency grid.}
The bubble convolution \cref{eq:sigma_phph_tau} has support up to
% REVIEW: [FLAG-ONLY] mixed omega convention -- f_max >= 2*omega_max/2pi carries an explicit 1/2pi (angular) while |Sigma^R|/omega_max^2 and the THz^2 magnitudes treat omega as ordinary THz
$2\omega_{\max}$, so the grid must satisfy $f_{\max}\ge2\omega_{\max}/2\pi$
(\cref{eq:conv_support}). An under-ranged grid aliases the truncated tail back
into the band as a spurious strong-coupling self-energy that destabilises the
SCBA: for the \texttt{d5a} wire the historical $f_{\max}=18$\,THz, far below the
% REVIEW: 2*63 = 126, not 129
$\sim126$\,THz convolution support set by the $63$\,THz Si--H stretch, inflates
$|\Sigma^R|$ by $\sim25\times$ and diverges the multi-slab iteration, whereas the
properly-ranged grid returns it to the
perturbative regime ($|\Sigma^R|/\omega_{\max}^2\approx0.2\%$). The one-shot
verification across bulk Si and \texttt{d5a} is tabulated in
\cref{tab:fmax_aliasing_one_shot}; the solver auto-extends $f_{\max}$ by default.

\paragraph{Self-energy cutoffs.}
The FC3-magnitude threshold, the Green's-function range, and a diagonal-$G$
approximation inside the bubble were swept on the open nanowires
(\cref{fig:res_cutoffs}) and on the carbon nanotube (\cref{fig:res_cnt_cutoff}).
The dominant cutoff is the off-diagonal Green's-function content: a diagonal-$G$
approximation is catastrophic on the open wires ($5$--$30\times$), while the
magnitude threshold converges quickly. The closed tube is far more forgiving --
all corners of the cutoff cube lie within $\pm10\%$ of the full-coupling value.

\paragraph{Vertex compression.}
The low-rank FC3 decompositions of \cref{sub:fc3_compression} trade parameter count
for accuracy (\cref{fig:res_decomp}): the transport observable converges faster
than the Frobenius residual the fitters minimise, but the matricised SVD becomes
unphysical at higher rank while the symmetric ans\"atze degrade gracefully.

\todo[inline]{Consolidate the three sweeps above into one approximation-cost figure
(conductance / $G_{\rm anh}-G_{\rm ball}$ vs.\ each approximation, across
% REVIEW: removed dangling reference to non-existent document/MISSING.md
\texttt{cnt33}/\texttt{d5a}/\texttt{d11a}/film).}
